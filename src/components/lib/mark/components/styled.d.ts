export type ChildrenProp = {
  children: React.ReactNode
}

export type OpenGuideProps = {
  screen: string
}

export type BackgroundColor = {
  backgroundC: string
}

export type PaginationProps = {
  handleNext: (e) => void
  handlePrevious: (e) => void
  handleSkip: (e) => void
}
